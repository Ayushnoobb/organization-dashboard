import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}
const assFaqSlice = createSlice({
    name: "a",
    initialState,
    reducers: {}
});
export const { } = assFaqSlice.actions

export default assFaqSlice.reducer